# API Environment Configuration

# API Environment Mode
# Options: 'localhost' or 'render'
# - 'localhost': For local development (backend running on your machine)
# - 'render': For production (backend deployed on Render)
# Change this to switch between local development and production
# For production builds, set to 'render'
# For local development, set to 'localhost'
EXPO_PUBLIC_API_ENV=render

# Local Development API URL
# Use this when running backend locally
EXPO_PUBLIC_API_BASE_URL_LOCAL=http://localhost:3000

# Production API URL (Render deployment)
# This is your Render backend URL
EXPO_PUBLIC_API_BASE_URL_RENDER=https://dorsu-connect.onrender.com
#EXPO_PUBLIC_API_BASE_URL_RENDER=https://dorsu-connect-clean.onrender.com

# Firebase Configuration (Optional - defaults are in firebase.ts)
# Uncomment and set these if you want to override Firebase config
#EXPO_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key
#EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
#EXPO_PUBLIC_FIREBASE_PROJECT_ID=your_project_id

# Google Sign-In Client IDs (Optional - defaults are in authService.ts)
#EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your_web_client_id
#EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=your_android_client_id

# IMPORTANT: Firebase Email Template Configuration
# The email verification link's continueUrl is configured in Firebase Console, not here.
# Go to Firebase Console → Authentication → Templates → Email address verification
# Set Action URL to: dorsuconnect://verify-email (for mobile) or your Render URL
# This prevents emails from showing localhost URLs